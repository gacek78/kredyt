services:
  kredyt:
    build: .
    ports:
      - "8082:3000"
    volumes:
      - kredyt_data:/app/data   # baza SQLite przeżywa restarty i aktualizacje obrazu
    environment:
      - PORT=3000
    restart: unless-stopped

volumes:
  kredyt_data:
